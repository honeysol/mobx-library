{
  "name": "mobx-demand",
  "version": "1.0.0-alpha.13",
  "description": "This library provides a decorator version of MobX onBecomeObserved and a decorator to make it more convenient.",
  "author": "norami",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/honeysol/mobx-library.git",
    "directory": "packages/mobx-demand"
  },
  "scripts": {
    "start": "tsdx watch --tsconfig tsconfig.build.json --verbose --noClean",
    "build": "tsdx build --tsconfig tsconfig.build.json",
    "test": "tsdx test",
    "lint": "tsdx lint",
    "prepublish": "npm run build"
  },
  "main": "dist/index.js",
  "module": "dist/mobx-demand.esm.js",
  "typings": "dist/index.d.ts",
  "files": [
    "README.md",
    "dist"
  ],
  "peerDependencies": {
    "mobx": ">=5"
  },
  "devDependencies": {
    "mobx": "^6.1.8"
  },
  "dependencies": {},
  "publishConfig": {
    "access": "public"
  }
}
