{
  "name": "mobx-autoclose",
  "version": "1.0.0-alpha.15",
  "private": true,
  "description": "automatically release the resource with MobX",
  "author": "norami",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/honeysol/mobx-library.git",
    "directory": "packages/mobx-autoclose"
  },
  "scripts": {
    "start": "tsdx watch --tsconfig tsconfig.build.json --verbose --noClean",
    "build": "tsdx build --tsconfig tsconfig.build.json",
    "test": "tsdx test",
    "lint": "tsdx lint",
    "prepublish": "npm run build"
  },
  "main": "dist/index.js",
  "module": "dist/mobx-autoclose.esm.js",
  "typings": "dist/index.d.ts",
  "files": [
    "README.md",
    "dist"
  ],
  "peerDependencies": {
    "mobx": ">=5"
  },
  "devDependencies": {
    "mobx": "^6.1.8"
  },
  "dependencies": {
    "mobx-annotation-manipulator": "^1.0.0-alpha.15",
    "mobx-monitor": "^1.0.0-alpha.15"
  },
  "publishConfig": {
    "access": "public"
  }
}
